{
  "name": "tollgate-tracker-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch main.ts",
    "start": "bun run main.ts",
    "build": "bun build main.ts --outdir dist --target bun",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "type-check": "bun tsc --noEmit"
  },
  "dependencies": {
    "elysia": "^1.1.29",
    "@elysiajs/cors": "^1.1.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "latest",
    "typescript": "^5.9.3"
  }
}
